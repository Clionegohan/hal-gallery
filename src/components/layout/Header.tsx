const Header = () => {
  return (
    <header
      className="fixed top-0 left-0 right-0 z-40 border-b border-zinc-800 bg-black/80
                     backdrop-blur-md px-4 py-3"
    >
      <div className="font-semibold text-zinc-100">hal gallery</div>
    </header>
  );
};

export default Header;
